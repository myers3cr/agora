<%= form_for ([:admin, @currency]) do |f| %>
  <% if @currency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@currency.errors.count, "error") %> prohibited this currency from being saved:</h2>
      <ul>
      <% @currency.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<fieldset>
	<legend><%= legend %></legend>
  <div class="field">
    <%= f.label :iso_code, "ISO code" %>
    <%= f.text_field :iso_code, autofocus: "autofocus" %>
  </div>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>
</fieldset>

  <div class="actions">
    <%= f.submit "Save" %>&nbsp;<%= link_to 'Cancel', admin_currencies_path %>
  </div>
<% end %>
